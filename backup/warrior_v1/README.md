# warrior_v1

旧 warrior モデル(1ボーン・全面 UV [0,0] の簡易人型)。v2(tools/gen_warrior.js 生成)に
差し替える前の状態。

- `warrior.geo.json` / `warrior.entity.json` … v1 の中身そのまま。
- `warrior.png` … v1 のテクスチャ(171B、ほぼ単色)は差し替え時に退避し損ねたため無い。
  v1 に戻したい場合は 64x64 の単色 PNG を置けば見た目は当時とほぼ同じになる。
